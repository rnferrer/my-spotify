import { createTheme } from "@mui/system";

export const colorTheme = createTheme({
  palette:{
    primary: {
      main: '#5fcd5e',
      light: '#7fd77e',
      dark: '#428f41',
      contrastText: '#fff'
    }
  }
})